/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/lsb-e-diaz-real-estate-associates-inc-hialeah',
  images: { unoptimized: true },
};
module.exports = nextConfig;
